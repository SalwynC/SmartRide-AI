# ============================================
# SmartRide-AI Environment Configuration
# ============================================
# Copy this file to .env and fill in your real values:
#   cp .env.example .env
#
# NEVER commit .env — it contains real secrets!
# ============================================

# Database Configuration
# For local PostgreSQL:
#   DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/smartride
# For Neon (cloud):
#   DATABASE_URL=postgresql://user:pass@ep-xyz.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=

# Session Configuration
# Generate a strong random secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=

# Server Configuration
PORT=5000
NODE_ENV=development
