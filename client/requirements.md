## Packages
recharts | Dashboard analytics charts (Line, Bar, Pie)
framer-motion | Smooth transitions and entry animations
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
- Using CSS Grid for the Zone Heatmap (robust, no complex map SDK setup required).
- "View as" role switcher implemented for simulation purposes.
- Dashboard analytics use Recharts.
- UI components (shadcn-like) are expected to be available or standard HTML/Tailwind used.
